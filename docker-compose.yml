services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./output:/app/output
      - ./assets:/app/assets
      - ./src:/app/src
    environment:
      - BLENDER_PATH=/opt/blender/blender
      - LOG_LEVEL=INFO
    restart: unless-stopped
